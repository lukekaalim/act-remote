// @flow strict
/*::
import type { JSONDiff, JSONValue } from "@lukekaalim/act-remote";
import type { CommitID } from "@lukekaalim/act-reconciler";
*/

/*::
export type RendererService = {
  submitDiff: (diff: JSONDiff) => void,
  subscribeCallback: (
    callback: (commit: CommitID, prop: string, args: JSONValue[]) => void,
  ) => { cancel: () => void };
};
*/